name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubunut-latest
        steps:
        - name: Welcome Message
          run: echo "My first GitHub Actions Job"


        - name: List files
          run: ls

        - name: Read file
          run: cat README.md
